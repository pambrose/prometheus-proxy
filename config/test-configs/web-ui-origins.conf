// Config for ProxyWebDashboardTest's Origin spec: web-ui.conf plus one allowed origin. A file rather than a -D
// override, because -D values parse with properties syntax and a list would arrive as a string.

include "web-ui.conf"

proxy {
  dashboard {
    allowedOrigins = ["https://dash.example.com"]
  }
}
