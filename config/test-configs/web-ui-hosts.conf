// Config for ProxyWebDashboardTest's Host allowlist spec: web-ui.conf plus one allowed host name. A file rather than a
// -D override, because -D values parse with properties syntax and a list would arrive as a string.

include "web-ui.conf"

proxy {
  dashboard {
    allowedHosts = ["dash.internal"]
  }
}
