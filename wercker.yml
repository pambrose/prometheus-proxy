box: java

dev:
  steps:
    - script:
      name: Build
      code: |
        mvn -DskipTests=true clean package

build:
  steps:
    - script:
      name: Build
      code: |
        mvn -DskipTests=true clean package
