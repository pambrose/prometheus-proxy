box: java

dev:
  steps:
    - script:
      name: Build
      code: |
        apt-get install -y maven
        mvn -DskipTests=true clean package

build:
  steps:
    - script:
      name: Build
      code: |
        apt-get install -y maven
        mvn -DskipTests=true clean package
