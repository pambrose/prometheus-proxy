box: java

dev:
  steps:
    - script:
      name: Build
      code: |
        ./mvnw -DskipTests=true clean package

build:
  steps:
    - script:
      name: Build
      code: |
        ./mvnw -DskipTests=true clean package