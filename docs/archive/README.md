# Archive

Point-in-time documents kept for their history: code reviews whose findings are all resolved, the July 2026
feature proposals, and the design specs and implementation plans written while building those features
(`superpowers/`). Each describes the code as it was when it was written, so line numbers and names may have
moved on. The living documentation is one level up, in `docs/`.
