agent {
  name = "test-agent"

  proxy {
    hostname = "proxy-host"
    port = 50051
  }

  pathConfigs: [
    {
      name: "Large metrics"
      path: large_metrics
      url: "http://metrics-stub/large"
    }
  ]

  scrapeTimeoutSecs = 30

  internal {
    reconnectPauseSecs = 1
  }
}
