server {
  listen 443 ssl;
  server_name waterzooi.test.google.be;

  ssl_certificate     /etc/nginx/certs/server.pem;
  ssl_certificate_key /etc/nginx/certs/server.key;

  location / {
    root /usr/share/nginx/html;
  }
}
