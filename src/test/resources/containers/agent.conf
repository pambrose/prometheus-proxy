agent {
  name = "test-agent"

  proxy {
    hostname = "proxy-host"
    port = 50051
  }

  pathConfigs: [
    {
      name: "Test metrics"
      path: test_metrics
      url: "http://metrics-stub/metrics"
    }
  ]

  scrapeTimeoutSecs = 5

  internal {
    reconnectPauseSecs = 1
  }
}
